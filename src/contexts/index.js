// Import the TodoContext, TodoProvider, and useTodo hook from TodoContext
import { TodoContext, TodoProvider, useTodo } from "./TodoContext";

// Export the imported context, provider, and hook for use in other parts of the application
export { TodoContext, TodoProvider, useTodo };
